import { createRef } from "react";

export const inputQuestionRef = createRef<HTMLInputElement>();
export const textAreaAnswerRef = createRef<HTMLTextAreaElement>();
export const inputAnswerUrlRef = createRef<HTMLInputElement>();
export const inputAnswerFileRef = createRef<HTMLInputElement>();
